Тест переинициализированного репозитория. Перешел на Linux.

Прописал следующую последовательность команд:
```
git clone <url>
git init
git remote add origin <url> 
git fetch origin
git reset --mixed origin/master
```
